<li>
	<%= link_to product.name, edit_admin_product_path(product), class: 'product_name' %>
	<%#= link_to "delete", destroy_admin_product_path(product), :method => :delete,
	 		:confirm => "Are U sure?",
	 		:title => "Delete #{product.name}" %>
</li>
